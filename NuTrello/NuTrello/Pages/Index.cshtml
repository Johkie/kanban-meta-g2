@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-left">
    Coola saker som knappar och listor här
    
</div>
<div class="text-primary text-center">
    Här ska det vara en board så småningom
    <br>
    @* if boards are empty, render a button to add a board *@
    @if (Model.boards == null)
    {
        <button class="createBoard" asp-area="" asp-page="/Boards/2">Create a Board</button>
    }
    else{
        //  INTE I MVP: foreach board, rendera en button för varje bräde.
        // Visa brädets knapp
        <div>
            <button id="2" onclick="/Boards">Brädknapp</button>
        </div>

<div class ="lägg-till-board-knapp">
    <h1>Board</h1>
    <img src="~/img/plus-circle-solid.svg" weight="64" height="64" onclick="insertList()" />

</div>
    }
</div>